import { createStyles } from "@material-ui/core";
import { primaryGrey } from '../commonStyles';

const MuiAppBar = createStyles({
    colorPrimary: {
        backgroundColor: primaryGrey,
    },
});

export default MuiAppBar;